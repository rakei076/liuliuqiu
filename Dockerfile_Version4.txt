FROM python:3.10-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 拷贝源码
COPY . .

# 监听端口
ENV PORT=80
EXPOSE 80

# 启动。先用内置服务器跑起来，后面需要再换 gunicorn 也行
CMD ["python", "app.py"]